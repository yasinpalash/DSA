
/**
 * Scenario: A company wants to count the number of users who have logged in today.
 * They store user IDs in an array and need to count how many times each user appears.
 *Problem Statement:
  Given an array of user IDs, count the frequency of each unique user.
 */
